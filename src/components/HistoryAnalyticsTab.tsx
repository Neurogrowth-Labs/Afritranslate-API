import React, { useEffect, useState } from "react";
export default function HistoryAnalyticsTab({ token, onLoginPrompt }: { token: string | null; onLoginPrompt: () => void }) { const [history, setHistory] = useState<any[]>([]); useEffect(() => { if (!token) return; fetch("/api/v1/history", { headers: { Authorization: `Bearer ${token}` } }).then(r => r.ok ? r.json() : []).then(d => setHistory(Array.isArray(d) ? d : d.history || [])).catch(() => setHistory([])); }, [token]); if (!token) return <div className="rounded-3xl border border-slate-800 bg-slate-900/60 p-6"><h1 className="text-2xl font-black text-white">Audit Logs & Stats</h1><p className="mt-2 text-sm text-slate-400">Sign in to view request history and usage analytics.</p><button onClick={onLoginPrompt} className="mt-4 rounded-xl bg-amber-500 px-4 py-2 text-sm font-bold text-slate-950">Sign in</button></div>; return <div className="rounded-3xl border border-slate-800 bg-slate-900/60 p-6"><h1 className="text-2xl font-black text-white">Audit Logs & Stats</h1><div className="mt-6 space-y-3">{history.length ? history.map((item, idx) => <div key={item.id ?? idx} className="rounded-2xl border border-slate-800 bg-[#111827] p-4 text-sm"><p className="text-slate-200">{item.source_text}</p><p className="mt-2 text-amber-300">{item.translated_text}</p></div>) : <p className="text-sm text-slate-500">No history yet.</p>}</div></div>; }
