import React from "react";
export default function WebhookPlaygroundTab() { return <div className="space-y-6"><section className="rounded-3xl border border-slate-800 bg-slate-900/60 p-6"><h1 className="text-2xl font-black text-white">Webhook Playground</h1><p className="mt-2 max-w-3xl text-sm leading-6 text-slate-400">Prototype WhatsApp, Messenger, and generic webhook adapters before connecting production platform credentials. Voice notes should be downloaded from platform media URLs, processed through STT → MT → TTS, then sent back through the platform Send API.</p></section><div className="grid gap-4 md:grid-cols-3">{["/api/v1/webhook/whatsapp","/api/v1/webhook/generic","/api/v1/webhook/config"].map(route => <div key={route} className="rounded-2xl border border-slate-800 bg-[#111827] p-5"><p className="font-mono text-sm text-amber-400">{route}</p><p className="mt-3 text-xs text-slate-500">Use HTTPS in production and verify platform signatures on every inbound request.</p></div>)}</div></div>; }
