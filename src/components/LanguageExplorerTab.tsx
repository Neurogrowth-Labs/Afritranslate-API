import React from "react";
import { AFRICAN_LANGUAGES } from "../data/languages";
export default function LanguageExplorerTab({ onTryTranslate }: { onTryTranslate: (langCode: string) => void }) { return <div className="rounded-3xl border border-slate-800 bg-slate-900/60 p-6"><h1 className="text-2xl font-black text-white">Language Catalog</h1><p className="mt-2 text-sm text-slate-400">Supported language registry for STT, machine translation, and TTS availability.</p><div className="mt-6 grid gap-4 md:grid-cols-2 xl:grid-cols-3">{AFRICAN_LANGUAGES.map(l => <div key={l.code} className="rounded-2xl border border-slate-800 bg-[#111827] p-4"><div className="flex items-start justify-between"><div><h2 className="font-bold text-white">{l.name}</h2><p className="text-xs text-amber-400">{l.nativeName} • {l.region}</p></div><span className="rounded bg-slate-950 px-2 py-1 font-mono text-xs">{l.code}</span></div><div className="mt-4 flex gap-2 text-[10px] font-bold uppercase"><span className={l.stt ? "text-emerald-400" : "text-slate-600"}>STT</span><span className={l.mt ? "text-emerald-400" : "text-slate-600"}>MT</span><span className={l.tts ? "text-emerald-400" : "text-slate-600"}>TTS</span></div><button onClick={() => onTryTranslate(l.code)} className="mt-4 text-xs font-bold text-amber-400 hover:text-amber-300">Try translate →</button></div>)}</div></div>; }
